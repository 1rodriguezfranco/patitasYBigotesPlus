<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %> 

<body>

    <%- include("../partials/header") %>

    <main class="userProfile">

        <section class="top-userProfile">
            <img src="/images/avatars/<%= user.avatar %>">
            <br><br>
            <p class="userName"><%= user.name %></p>
            <br>
            <p class="userEmail"><%= user.email %></p>
            <br>
            <button type="button" class="close-session"><a href="/auth/edit/<%= user.id %>">Editar datos</a></button>
        </section>
        <br>
        <form action="/auth/profile" method="POST">
            <button type="submit" class="close-session">Cerrar sesión</button>
        </form>

    </main>

    <% if(user.admin === 1){ %>
        <div class="return">
            <a href="/admin/table" class="returnAdminTable">Tablero de administrador</a>
        </div>
    <% } %>

    <%- include("../partials/footer") %>

</body>

</html>